User Story: As a System, I need to asynchronously generate a clinical summary using the Gemini API the moment a counselor escalates a case, so the report is instantly ready when the psychologist opens it.

Specific Description: Create a background job (e.g., GenerateGeminiReferralSummaryJob) triggered immediately after the Guru BK selects "Perlu Rujukan Psikolog" in Feature 6.

Data Aggregation: Fetch the student's consented raw data (incidents, counseling_logs, Flask NLP flags).

API Configuration: Integrate the Gemini REST API or PHP SDK using the gemini-1.5-flash model. Hardcode the parameters: temperature: 0.1 and maxOutputTokens: 350.

Prompt Engineering: Inject the exact system_instruction forbidding clinical diagnoses and enforcing the 200-word limit in formal Indonesian. Map the aggregated data into the User Prompt template.

Storage: Save the API response as a static text string into the referral_summaries table along with the original raw JSON payload. Ensure server-side truncation is applied if the output exceeds 200 words.

DoD: The background job successfully communicates with the Gemini API, strictly obeys the prompt constraints, and stores the factual, non-diagnostic summary in the database without halting the main user request lifecycle.

