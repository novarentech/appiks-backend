User Story: As a Developer, I need a database table to store the psychologist's published availability independently from actual appointment bookings.

Specific Description: Create a database migration for a new table named psychologist_slots.

Columns required: id (primary key), psychologist_id (foreign key), slot_date (date), slot_start_time (time), slot_end_time (time), status (enum: 'available', 'tentative', 'confirmed' - default 'available'), and standard timestamps.

DoD: The migration runs successfully, and the Eloquent model relationships are defined.

