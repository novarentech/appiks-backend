User Story: As a System, I want to calculate the time elapsed since a Red Zone trigger, so the front-end can visually highlight cases where the Guru BK is unresponsive.

Specific Description: In the Principal's incident listing endpoint, append an SLA calculation logic. Compare the created_at timestamp with the acknowledged_at timestamp of the Guru BK. If acknowledged_at is null and now() exceeds the 2-hour mark, attach a boolean flag "is_sla_breached": true to that specific incident's JSON payload.

DoD: The endpoint successfully calculates and returns the SLA breach flag accurately based on the 24-hour logic.

